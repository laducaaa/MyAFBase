# Copy to keystore.properties (never commit the real file).
# Generate with:
# keytool -genkey -v -keystore myafbase-upload.jks -keyalg RSA -keysize 2048 -validity 10000 -alias upload
storeFile=../myafbase-upload.jks
storePassword=CHANGE_ME
keyAlias=upload
keyPassword=CHANGE_ME
