import SwiftUI

enum HomeMetrics {
    static let heroBackground = AppTheme.heroBackground
    static let heroCornerRadius = AppTheme.heroCornerRadius
    static let heroShadowOpacity = AppTheme.heroShadowOpacity
    static let heroSecondaryText = AppTheme.heroSecondaryText
    static let heroDivider = AppTheme.heroDivider
}
